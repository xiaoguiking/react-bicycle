import React from 'react';
import renderEmpty from 'antd/lib/config-provider/renderEmpty';

export default class DemoTem extends React.Component {
  render() {
    const style = {
      width: 'calc(85vw)',
    };
    return <div style={style}>demo</div>;
  }
}
